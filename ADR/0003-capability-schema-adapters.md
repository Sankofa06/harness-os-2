# ADR 0003 — Capability/schema adapters

Status: Accepted

Do not force providers/engines into false parity. Each adapter declares capabilities and a settings schema. Shared UI renders normalized common controls plus provider-specific sections.
